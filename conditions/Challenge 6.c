#include <stdio.h>
#include <stdlib.h>

int main()
{

    int N;
        printf("veuillez entrer la valeur de N ");
        scanf("%d",&N);
    if (N>0)
        printf("ce nombre est positif");
    else if (N<0)
        printf("ce nombre est negatif");
    else
        printf("ce nombre set nul");



    return 0;
    }
