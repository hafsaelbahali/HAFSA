#include <stdio.h>
#include <stdlib.h>

int main()
{

    int age ;
        printf("veuillez entrer l'age : ");
        scanf("%d",&age);
     if (age <= 18)
        printf("entrer la categorie est vous n etes pas eligible pour voter");
     else
        printf("entrer la categorie est vous etes eligible pour voter");



    return 0;
    }
